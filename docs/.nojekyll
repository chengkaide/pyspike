# Served by GitHub Pages with Jekyll disabled: this folder is a set of
# standalone files (two HTML documents and a JSON catalogue) that GitHub Pages
# should copy verbatim.  Nothing here is a Jekyll site, and running Jekyll over
# them would only add a build step that can fail.
